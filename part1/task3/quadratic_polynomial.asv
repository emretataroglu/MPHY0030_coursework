function [f,fsv] = quadratic_polynomial(x,a)
syms x1 x2 x3

% Function
% a(10) is a(0) 
f = @(x1, x2, x3) a(2).*x1.^2 + a(3).*x2.^2 + a(4).*x3.^2 + a(5)*x1*x2 + a(6)*x1*x3 + a(7)*x2*x3 + a(8)*x1 + a(9)*x2 + a(10)*x3 + a(1);

% Function scalar value
fsv = f(x(1),x(2),x(3));

end
